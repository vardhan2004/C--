#include<iostream>
using namespace std;
int main(){
    // pair with declaration
    pair<int, int>s = {1, 2};
    cout << s.first << " " << s.second;

    // pair with vectors
    //we can pair any of the data types
    //it should containe 2 only

    //vector pair
    vector <pair<int, int>>vp = {{1, 2, 3}, {1, 2, 3}};
    for(int i = 0; i)k


}