#include<iostream>
#include<map>
using namespace std;
int main(){
    //address are not arranged in continously
    // map<int, int>M;
    // M[1] = 10;
    // M[2] = 20;
    // map<int, int>::iterator it;
    // it  = M.begin();
    // it++;
    // cout << (*(it)).second;
    // cout <<it->second;
    map<char, int>m;
    string s;
    getline(cin, s);    

    return 0;

}