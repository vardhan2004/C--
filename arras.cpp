#include<iostream>
#include<algorithm>
#include<string>
using namespace std;
// the std namespace contains the built-in classes and declared functions.
int main(){
    // int arr[6] = {6,2,1,8,3,110};
    // int len = sizeof(arr)/sizeof(int);
    // // int key = 6;
    // // auto it = find(arr, arr+len, key);
    // // int ind = it - arr;
    // // cout << ind;
    // // cout << arr[ind];
    // sort(arr, arr+len); 
    // for(int i = 0; i < 6; i++){
    //   cout<<arr[i]<<" ";
    // };
    // cout << endl;
    // int key = 1;
    // auto it  = binary_search(arr, arr+len, key);
    // if(it == 1){
    //   cout << "present"<< endl;
    // }
    // else{
    //   cout << "Not Present";
    // }
    int arr[10] = {1,1, 2, 2, 3, 3, 3, 5, 5, 5};
    string s  = "hai";
    cout << s.size();
    // int len = sizeof(arr)/sizeof(int);
    // int key = 2;
    // auto lb = lower_bound(arr, arr+len, key);
    // auto ub = upper_bound(arr, arr+len, key);
    // int lin = lb - arr;
    // int uin = ub - arr;

    // cout << lin << " " <<uin;
    // int arr[10];
    // for(int i = 0; i < 10; i++){
    //   cin >> arr[i];
    // }
    // for(int i = 0; i < 10;  i++){
    //   cout << arr[i] << " ";
    // }


}